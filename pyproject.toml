[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [
  ".venv",
  "lambda/.venv",
  "cdk/node_modules",
  "cdk/cdk.out",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = [
  "E501",
]
